const numbers = [1, 2, 3, 4, 5, 6, 7, 8];

// Your code below
const doubled = numbers.map((n) => n * 2);
const halfed = numbers.map((n) => n / 2);
const squared = numbers.map((n) => n * n);

console.log(doubled);
console.log(halfed);
console.log(squared);
